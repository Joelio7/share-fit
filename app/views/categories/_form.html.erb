<%= form_for(@new_category) do |f| %>
    <%= f.text_field :body, class: 'form-control', placeholder: "Enter topic name" %>
    <div class="form-group">
    <%= f.submit "Save", class: 'btn btn-success' %>
   </div>

    <%end%>
